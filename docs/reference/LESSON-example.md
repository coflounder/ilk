---
id: lesson-example
title: An example lesson
status: current
updated: 2026-01-01
covers: []
encoded_as: none
---

# An example lesson

<!--
  Seeded by ilk as a worked example. Rename it, rewrite it, or delete it — ilk
  will not touch this file again.

  `encoded_as:` is the field that matters. It names what actually changed as a
  result of this lesson: a check id, a skill, a document, a layer version.
  `pending` (with a reason) and `none` (with a reason) are both honest answers;
  silence is not, which is why the check requires the field.
-->

## What happened

The facts, briefly. Not the narrative — the shape of the problem, so somebody can
recognise it happening again.

## Why it happened

The condition that made it possible, not the person who tripped over it. If the
answer is "somebody forgot", keep going: what made forgetting sufficient to cause
damage?

## What changed

The durable change, named in `encoded_as:` above. If the answer is "we will be more
careful", nothing has changed and this lesson will be learned again.
