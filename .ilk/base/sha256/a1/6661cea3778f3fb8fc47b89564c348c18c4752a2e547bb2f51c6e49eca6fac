#!/bin/sh
# Generated by ilk. Do not edit — run `ilk apply` to regenerate, or edit the layer that produced it.
#
# Runs the pre-push hooks declared by the layers in .ilk/config.yaml.
# Bypass in an emergency with `git commit --no-verify`.
set -eu

if ! command -v ilk >/dev/null 2>&1; then
	echo "ilk: not installed — skipping pre-push hooks (see https://github.com/coflounder/ilk)" >&2
	exit 0
fi

exec ilk hook run pre-push
