package legacy

// The code the notice covers, still present. `covers:` is matched against this,
// which is what makes the deprecation measurable rather than a reminder.
func Session() string { return "legacy" }
