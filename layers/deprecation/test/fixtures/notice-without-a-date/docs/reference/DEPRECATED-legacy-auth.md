---
id: dep-legacy-auth
title: Legacy auth is being removed
status: current
updated: 2020-01-01
covers:
  - src/auth/legacy/**
---

# Legacy auth is being removed

Everything a deprecation needs except the two dates: no `remove_after:`, so nothing
can ever hold it to one, and no `announced:`, so an extension would leave no trace.
